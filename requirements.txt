# ---- core ----
numpy>=1.24
pyyaml>=6.0
typer>=0.12
rich>=13.7
tqdm>=4.66

# ---- video / audio io ----
ffmpeg-python>=0.2.0
moviepy>=1.0.3
opencv-python>=4.9
librosa>=0.10
soundfile>=0.12

# ---- detection ----
scenedetect>=0.6.4          # opencv backend provided by opencv-python above
easyocr>=1.7.1

# ---- transcription (commentary) ----
# faster-whisper is lighter than whisperx; whisperx adds word-level align.
faster-whisper>=1.0.0
# whisperx>=3.1.1   # optional, uncomment for word-level commentary timing

# ---- vision / tracking ----
# NOTE: install torch/torchvision separately matched to your CUDA (see docs/SETUP.md)
ultralytics>=8.2.0
supervision>=0.22.0
scikit-learn>=1.3          # team-classification clustering (vision/teams.py)
huggingface-hub>=0.23      # automatic model downloads (src/modelhub.py)

# ---- web UI ----
# Pinned to the 4.x line: the WebUI (app/webui.py) is written against the
# Gradio 4 API. Unpinned, pip resolves to 6.x which has breaking changes.
gradio>=4.44,<5

# ---- misc ----
scipy>=1.11
pillow>=10.0
